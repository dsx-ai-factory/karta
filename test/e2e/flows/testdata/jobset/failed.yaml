# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 NVIDIA Corporation
#
# A JobSet whose replicated Job fails: the pod runs briefly then exits non-zero with no retries
# (backoffLimit 0 and the default failure policy), so the Job fails and the JobSet reports Failed.
# The short Running window while the pod executes is declared Optional in the flow.
apiVersion: jobset.x-k8s.io/v1alpha2
kind: JobSet
metadata:
  name: karta-e2e-jobset-failed
  namespace: default
spec:
  replicatedJobs:
    - name: workers
      replicas: 1
      template:
        spec:
          parallelism: 1
          completions: 1
          backoffLimit: 0
          template:
            spec:
              restartPolicy: Never
              containers:
                - name: worker
                  image: busybox:1.36
                  command: ["sh", "-c", "sleep 5; exit 1"]
                  resources:
                    requests: {cpu: 50m, memory: 32Mi}
